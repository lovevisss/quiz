2026-04-17: Fixing Playwright selector failures on /quiz can require aligning the root inertia route to the page/component containing the expected data-testid, not just matching the filename or route name.
2026-04-17: F2 REJECT fixes - Deduped survey-question routes in web.php (kept only one per action, preserved both naming conventions for index/create/store/edit/update/destroy). Wrapped fetchUser dispatch in app.ts with .catch to prevent unhandled rejections. Added deterministic negative-path E2E for /quiz/nonexistent (asserts 404/fallback UI, not flake-prone). All tests and build pass on both mobile configs.
